# Arch-spec 0.1.0 — cidade + guerreiro

## Waves

| Wave | Etapa | Ownership | DoD |
|------|--------|-----------|-----|
| A | core + math + ecs + loop + contracts | `src/core/**` | testes unit ecs/loop/math |
| A∥ | platform keyboard + canvas renderer | `src/platform/**` | typecheck |
| B | TileMap + `buildCityMap` + CityScene ground | `src/game/world/**`, `src/game/scenes/**` | mapa 2×2 renderiza |
| C | directions, components, input intent, movement, collision, anim idle/walk | `src/game/**` | 8-dir move |
| D | attack + death + WeaponDef | `src/game/weapons/**`, systems | Space attack, death |
| E | main wiring | `src/main.ts`, `index.html` | jogável |
| F | architecture check + vitest smoke | `tools/`, `tests/` | gates |

## APIs congeladas

- `Direction` + `directionFromVelocity` (8-way)
- `WeaponDef` + registry `starter_sword`
- `TileMapData` / `SpriteAtlasData` contracts
- Intent: `{ moveX, moveY, attackPressed, killPressed }`

## System order

10 input → 20 movement → 30 collision → 40 anim state → 50 anim playback → 60 camera → 100 render

## Fora

Netcode, inventário UI, AI, biomas.
