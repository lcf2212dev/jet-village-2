# Spec 0.1.2 — Guerreiro com sheet de espada

| Campo | Valor |
| ----- | ----- |
| Versão | **0.1.2** |
| Status | **Implementada** |
| Depende de | 0.1.1 (tiles de chão) |

## Escopo

- Sheet do guerreiro com espada (promovido de geração OpenAI): idle / walk / attack / death em 7 direções no atlas (+ fallback `up-left`).
- Render via `drawSprite` + `SpriteAtlasData` com `startFrame` para strips horizontais.
- Guerreiro placeholder geométrico só como fallback se atlas/textura falharem.

## Fora

- Run como estado separado (usa walk).
- Hit reaction clip.
- Slice perfeito pixel-a-pixel do sheet AI (grid 21×7 aproximado).
